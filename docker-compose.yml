
services:
  postgres:
    restart: always
    build:
      context: ./docker/postgres
      dockerfile: Dockerfile
    image: transcendence_postgres:latest
    container_name: postgres
    env_file:
      - .env
    ports:
      - "5432:5432"
    networks:
      - transcendence
    volumes:
      - postgres_data:/var/lib/postgresql/data/

  backend_user:
    depends_on:
      - postgres
    container_name: users
    build:
      context: .
      dockerfile: docker/backend_user/Dockerfile
    image: transcendence_backend_user:latest
    volumes:
      - ./users:/app
    ports:
      - "8001:8001"
    env_file:
      - .env
    networks:
      - transcendence
    restart: always

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    container_name: nginx
    image: transcendence_nginx:latest
    ports:
      - "3042:3042"
    volumes:
      - frontend_data:/usr/share/nginx/frontend
    networks:
      - transcendence
    restart: always
    depends_on:
      - backend_user

networks:
  transcendence:
    name: transcendence
    driver: bridge

volumes:
  postgres_data:
    driver: local
    driver_opts:
      type: none
      device: "/Users/asiercara/Documents/proyectos_separados/ft_transcendence42/data/postgres"
      o: bind
  frontend_data:
    driver: local
    driver_opts:
      type: none
      device: "./docker/nginx/frontend"
      o: bind